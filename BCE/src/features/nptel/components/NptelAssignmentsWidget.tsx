import Link from 'next/link'; import Card from '@/components/ui/Card';
export default function NptelAssignmentsWidget({ assignments }: { assignments: any[] }) { if (!assignments.length) return null; return <Card variant="glass" padding="lg"><div style={{ display: 'flex', justifyContent: 'space-between' }}><h2 style={{ margin: 0 }}>NPTEL Assignments</h2><Link href="/student/nptel" className="text-neon-cyan">View all →</Link></div><div style={{ display: 'grid', gap: 10, marginTop: 14 }}>{assignments.slice(0, 4).map(a => <div key={`${a.courseName}-${a.assignmentTitle}`}><strong>{a.assignmentTitle}</strong><div className="text-secondary">{a.courseName} • {a.status.replace('_', ' ')}</div></div>)}</div></Card>; }
