import { redirect } from 'next/navigation'; import Card from '@/components/ui/Card'; import { getCodeArenaActor } from '@/features/code-arena/server'; import CodingAccounts from '@/features/code-arena/components/CodingAccounts';
export default async function CodingProfilePage(){const {supabase,user}=await getCodeArenaActor();if(!user)redirect('/login');const [{count:solved},{count:battles},{data:accounts}]=await Promise.all([supabase.from('coding_submissions').select('problem_id',{count:'exact',head:true}).eq('student_id',user.id).eq('status','ACCEPTED'),supabase.from('coding_battle_participants').select('*',{count:'exact',head:true}).eq('student_id',user.id),supabase.from('student_external_accounts').select('platform,username,rating,max_rating,rank,profile_url').eq('student_id',user.id)]);return <div style={{display:'flex',flexDirection:'column',gap:'var(--space-xl)'}}><div><h1 className="text-gradient">Coding Profile</h1><p className="text-secondary">Your BCE coding history and private public-profile connections.</p></div><div className="dashboard-stats-grid"><Card variant="glass" className="stat-card"><div><strong>{solved||0}</strong><p>BCE Problems Solved</p></div></Card><Card variant="glass" className="stat-card"><div><strong>{battles||0}</strong><p>Battles Played</p></div></Card></div><CodingAccounts initial={accounts||[]} /></div>}
