# 14-Day Full Stack Curriculum & Admin Enhancements Plan

You have provided an excellent, highly structured 14-day curriculum flow along with new requirements for the Admin Dashboard and advanced gamification rules. Here is the technical plan to implement this flow into your SkillArena platform.

## Proposed Changes

### 1. 📚 Course Curriculum Seeding
We will create an automated script (`seed_curriculum.js`) to insert the entire 14-day flow into your database. 
- **Course**: "Full Stack Web Development Mastery"
- **Lessons**: Day 1 to Day 14 covering HTML, CSS, JS, Git/GitHub, React, Node, MongoDB, Auth, and Deployment.
- **Assignments**: Each day will have an associated assignment (e.g., "Landing page + signup form UI").
- Tasks that require GitHub uploads or Deployment links will have `requires_github = true` and `requires_deploy = true` set in the database.

### 2. 👨‍🎓 Admin Dashboard: Student Tracking
We will update the `src/app/(admin)/admin/students/page.tsx` (currently a placeholder) to display the comprehensive **Student Table**.
- **Columns**: Name, Email, XP, Level, Progress bar (%), Last active.
- We will fetch this data by joining the `profiles` table with the `enrollments` table.

### 3. 🎓 Admin Dashboard: Course Manager (CRUD)
We will update `src/app/(admin)/admin/courses/page.tsx` to include:
- A UI to view the list of courses.
- Forms to Add, Edit, and Delete Courses, Lessons, and Assignments directly from the Admin Panel.

### 4. 🎮 Advanced Gamification Rules (PRO TIP)
We will implement the specific rewards you mentioned:
- **GitHub Submission = XP Reward**: Ensure assignments that require GitHub links reward bonus XP upon submission.
- **Deployment Link = Badge Unlock**: Update the `checkBadges` server action so that submitting a live deployment link automatically unlocks a specific "Deployment Master" badge.
- **Admin Approval = Leaderboard Boost**: When an admin approves an assignment in the review panel, it will trigger the XP award, instantly boosting the student's leaderboard ranking.

---

> [!IMPORTANT]  
> **User Review Required**
> Do you want me to write the `seed_curriculum.js` script so you can instantly populate your live database with all 14 days of content? 
> Also, should we use a specific icon or logo for the "Deployment Master" badge?

Please approve this plan so I can start coding! 🚀
